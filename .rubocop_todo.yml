# This file is generated by running:
# bundle exec rubocop --auto-gen-config --auto-gen-only-exclude --no-exclude-limit
#
# It contains all current offenses that should be ignored for legacy code.
# New code and new offenses in existing files will still be checked.
#
# This file is intentionally minimal - run the command above to generate it properly.

AllCops:
  Exclude: []
